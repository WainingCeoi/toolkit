import React from 'react'
import { createRoot } from 'react-dom/client'

// Placeholder shell — the real UI lands with the frontend phase.
createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <h1>🧰 Toolkit</h1>
  </React.StrictMode>,
)
