# Frontend (Node). Paths are relative to frontend/.

node_modules/

# Build output
dist/
dist-ssr/

# Local env overrides (VITE_-prefixed vars ship to the browser — no secrets here)
.env.local
.env.*.local

# Test output
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
