# Whole-repo only — sub-project rules live in backend/.gitignore and frontend/.gitignore.

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Agent scratch
.claude/

# Legacy Streamlit-era root .env (backend/.env is the live config)
/.env

# Runtime data the app creates on first run: the subscription and torrent
# databases, plus aria2's session and log. Machine state, never source.
/data/
