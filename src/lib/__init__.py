"""Support packages for Toolkit pages.

Pages in ``src/pages/`` are single self-contained scripts. A tool that needs
more than one module keeps its engine in its own subpackage here, e.g.
``lib/subgen`` powers the Optimized-IP Subscription page.
"""
